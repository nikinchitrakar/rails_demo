<%= form_for Comment.new,remote: true, :url => post_comments_path(@post) do |f| %>
  
  <p>
  	<%= f.hidden_field :comment_id, comment.comment.id %>
    <%= f.label :comment %><br>
    <%= f.text_area :comment, class: "form-control" %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>